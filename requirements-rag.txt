# MCP
# <2 pin: mcp 2.0.0 renamed FastMCP to MCPServer and reworked the server API
# (upstream v1.x is maintenance-mode-only; see modelcontextprotocol/python-sdk
# release notes), which this codebase isn't written against.
mcp[cli]>=1.0.0,<2

# Web framework (MCP server transport + proxy calls to mock_services)
uvicorn[standard]>=0.32.0
httpx>=0.28.0

# AWS (get_ec2_host_metrics — real CloudWatch metrics for the EC2 host)
boto3>=1.35.0

# RAG
llama-index-core>=0.12.0
llama-index-vector-stores-chroma>=0.4.0
llama-index-embeddings-huggingface>=0.4.0
chromadb>=0.6.0
sentence-transformers>=3.0.0

# Utilities
python-dotenv>=1.0.0
pydantic>=2.0.0
